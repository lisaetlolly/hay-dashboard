# Dashboard 改动 + 待确认 总表

> 截至 2026-04-28
> 用途：所有这一轮做的事 / 为什么做 / 还要你或店长拍板的，一张表说清

---

## ✅ 已改完，可以验收

### 数据 / 计算逻辑类（最关键）

| # | 改了什么 | 原因 | 文件 | 验收方法 |
|---|---|---|---|---|
| 1 | **CTR 改加权** = SUM(clicks)/SUM(imps) | 之前店长指定算术均值，4 月真实 3.67% 被算成 5%+，决策偏差 | `js/components/AdsPage.js` `adsCtr` | 投放面板「推广 CTR」KPI ≈ 3.67% |
| 2 | **关键词去重 4 维 key** = (date, kid, scene, kname) | 之前只用 (date, name) 同名词在多场景被吞，4 月漏 24K（68%） | `etl/etl_load.py` `load_wxst_keyword` | TRUNCATE+ETL 后 fact_wxst_keyword 4 月 SUM ≈ 35,810 |
| 3 | **Barro Bowl & Plate 图回来** | 老 PID 7660181033346 是 typo，正确是 1020815058332，文件名是后者 | `server.py` img_map 加 typo 别名 | 单品页 Barro 显示图片 |
| 4 | **商品管理 24→25** | OFFICIAL Set 在 RAW 快照阶段冻结，API 拉到的新 PID（含 typo 修正后的）没刷进去 | `dashboard.html` Object.assign 后重灌 OFFICIAL | 商品管理列表 25 个 |
| 5 | **上期排行三层兜底** | API 返回空 → RAW 本地算 → 还空就用上期日期再调 ranking API | `js/components/OverviewPage.js` loadRank | 排行榜「上期」列有数 |
| 6 | **wxst_flat 加 clicks** | 让前端能算加权 CTR，不用 imps×ctr 反推 | `server.py` /api/raw-data | 浏览器 console RAW.wxst[0].clicks 有值 |
| 7 | **总投放 sanity guard** | 防御算法：max(sum, 各分量)，避免 5万 < 7万 视觉冲突 | `js/components/AdsPage.js` totalPaidSpend | F12 console 有 warn 时记下分量 |
| 8 | **Cotton Bag 564552361178 显示名字** | dim_product 没这条，server JOIN 不到名字 | `etl/bootstrap_recovery.sql` 补 INSERT INTO dim_product | 投放面板显示「Cotton Bag 帆布包」 |

### 任务面板（投放面板）

| # | 改了什么 | 原因 |
|---|---|---|
| 9 | **任务行加内联编辑**（状态下拉 + 备注 input + 保存/取消） | 之前任务卡只能加评论，不能改状态/备注 |
| 10 | **管理员全字段编辑 Modal** | admin 可以改 detail/owner/category/priority/status/note |
| 11 | **每行删除按钮**（小×号） | 之前不能删任务 |
| 12 | **批量分配 owner**（勾多个 → 选 owner → /api/tasks/bulk-update） | 员工换人 / 周期开始批量分配 |
| 13 | **新增任务按钮**（卡头一个，不是每行）| /api/tasks POST 创建到当前周期 |
| 14 | **新建周期按钮**（周期下拉边）| admin 直接建新周期 |
| 15 | **diff -100% 隐藏**（光合渠道无数据时）| 当期值=0/null 不显示无意义的 -100% |
| 16 | **任务行权限矩阵**：admin 全字段，负责人状态+备注，其他只读 | 和后端 PATCH /api/tasks/{id} 权限对齐 |

### 其他

| # | 改了什么 | 原因 |
|---|---|---|
| 17 | **AI 配置移到设置页** | AI 分析页只剩触发按钮，AI 配置项搬到 设置 → AI 配置 |
| 18 | **小红书笔记接 API** | 之前是前端 localStorage 暂存，刷新就丢；现在 fact_xhs_note 表 + POST /api/xhs-notes |
| 19 | **/api/raw-data 返回 xhs_notes / xhs_by_pid** | 之前 RAW 里小红书笔记是写死快照，现在 DB 实时 |
| 20 | **CTR 排行图缩小** | 进度条从 1fr 改成固定 100px，整张卡矮一截 |
| 21 | **三渠道（人群/关键词/短视频）一行** | 之前短视频独占一长条，奇怪 |
| 22 | **单品 3 个标签常驻**（小红书/任务/事件）| 之前没数据就不显示标签，没地方加 |
| 23 | **单品里加 +事件 / +任务 / +笔记 按钮** | 配合上面，权限够的人可以直接加 |
| 24 | **事件标签颜色精简**（红/蓝/灰 3 色系）| 之前 5 色和任务状态色冲突 |
| 25 | **单品任务用最新周期** | 之前固定 4.20-22；现在退回 task_period.is_current |
| 26 | **周期下拉隐藏 0 任务周期** | /api/task-periods 加 task_count 过滤 |
| 27 | **当前角色切换 toast** | 之前切了没反馈，现在显示「已切换到 X」+ 当前权限实时显示 |
| 28 | **总投放 KPI footer 加口径分解** | 显示「商品报表 ¥X 万｜短视频 ¥Y 万」让人看清两个口径 |
| 29 | **shengchao/wanting 去掉括号** | display_name 改成「声超」「婉婷」 |
| 30 | **权限自动关掉 bug 修** | store.js `users: initial.users` 强制覆盖了用户改过的权限 |
| 31 | **指标配置加警示** | 提示「目前是登记表，不影响显示」，避免误以为这里能控制 |
| 32 | **总投放 tooltip 重写** | 详细写两层口径（ROI 用花费 vs 全曝光花费）|

---

## ⚠️ 还要你 / 店长 / 品牌方拍板的（10 项）

| # | 待定项 | 选项 | 影响 | 谁来决 |
|---|---|---|---|---|
| A | **GMV 用「支付金额」还是「下单金额」** | A1: 支付金额（行业标准 / ¥219.5万）<br>A2: 下单金额（含未付款 / 数字虚高 25%+） | 所有 GMV 显示 + ROI 分子 | 你 + 店长 |
| B | **总览要不要加「净支付 / 退款 / 新客占比」KPI** | A1: 加（4 月退款率 31.85% 不显示太危险）<br>A2: 不加（界面更简洁） | 总览页布局 | 你 |
| C | **总投放 KPI 拆「ROI 口径花费 vs 全曝光花费」** | A1: 拆（和万象台后台 ROI 13.94 对得上）<br>A2: 不拆（保持单一数字简洁） | KPI footer | 你 |
| D | **上期对比逻辑** | A1: 等长前移（4.27-30 vs 4.23-26）<br>A2: 上一完整周（vs 4.20-26）<br>A3: 加切换器让用户选 | 排行榜环比、KPI 环比 | 你 + 店长（看周报口径）|
| E | **4.20-22 周期怎么处理** | A1: 改名 4.20-26（推荐）<br>A2: 新建 4.23-26 补洞<br>A3: 不动 | 任务复盘对照 | 你 |
| F | **Slit/Tray Table 是否合并 SPU** | A1: 合（同系列）<br>A2: 不合（不同款）<br>A3: 部分合 | 排行榜 + 类目拆分 | 品牌方 |
| G | **副 SKU 合并是否含「字母 Logo Cotton Bag」「Facet Cabinet 多色」等** | 见「SKU 合并问题清单」13 对 | ETL SPU_MAP | 品牌方 |
| H | **创意报表是否接入 ETL** | A1: 加（fact_wxst_creative + 创意 ROI 排行）<br>A2: 暂不加 | 投放面板新增板块 | 你 |
| I | **「事件」和「运营动作」是否合并成「动作中心」** | A1: 合并 + 加范围字段（global/single/multi）<br>A2: 保持两个独立 | 单品页 + 设置页 + 效果分析页 | 你 |
| J | **指标配置真接通**（让登记表驱动各页指标卡）| A1: 接通（工作量约半天）<br>A2: 现状（仅元数据登记）| 全站指标卡 | 你 |

---

## 🔧 已知 bug，但还没修

| # | 问题 | 现状 | 优先级 |
|---|---|---|---|
| K | **店铺 GMV 没含退款拆分** | 只显示毛 GMV ¥219.5 万，看不到净 GMV ¥149.6 万 | 🔴 高（待 #B 决定） |
| L | **CPC 没单独 KPI** | 隐式在「投放趋势」里，没有显式数 | 🟡 中 |
| M | **上期对比写死等长前移** | 任务 #D | 🟡 中 |
| N | **「我的任务」前端没接通** | 后端 /api/tasks/mine 在，前端没调 | 🟢 低（暂缓）|
| O | **AI 分析页悬浮问答** | 你提过右下角悬浮 Q&A，未做 | 🟢 低 |
| P | **直接 vs 间接成交分开看** | 万象台数据里有，dashboard 没拆显示 | 🟡 中 |

---

## 待你执行的命令清单（优先级从高到低）

### 立刻
```bash
# 1. push 这一晚的所有改动
cd /Users/jiayi/Downloads/数据库数据
git add -A
git commit -m "fix: CTR 加权 + 关键词去重 + AI 配置移设置 + 任务 CRUD + 一摞细节"
git push
```

### Render 自动部署后，到 Neon Console 跑：
```sql
-- A. 修关键词漏数据（重灌该表）
TRUNCATE fact_wxst_keyword;
-- 然后本地跑 ETL：
-- export DATABASE_URL='...'
-- cd etl && python3 etl_load.py

-- B. 改 display_name + 补 dim_product
UPDATE users SET display_name = '声超' WHERE username = 'shengchao';
UPDATE users SET display_name = '婉婷' WHERE username = 'wanting';

INSERT INTO dim_product (product_id, spu_id, title, category_l1, category_l2, inventory) VALUES
  ('564552361178',  '564552361178',  'Cotton Bag 帆布包',       '配饰', '包袋', 0),
  ('1020815058332', '1020815058332', 'Barro Bowl & Plate 碗盘', '配饰', '餐具', 0)
ON CONFLICT (product_id) DO UPDATE SET title=EXCLUDED.title, category_l1=EXCLUDED.category_l1;

-- C. 同事权限补全
UPDATE users SET permissions =
  '["task.view_all","task.edit_own","task.view_own","action.view","meeting.view","metric.view","product.view","event.create","xhs.create"]'::jsonb
WHERE username = 'jas';

-- D. 跑核心比例对账（验加购率/转化率/新客占比/退款率/客单价）
WITH P AS (SELECT '2026-04-01'::text AS s, '2026-04-26'::text AS e),
agg AS (SELECT SUM(pay_amount) AS gmv, SUM(visitors) AS uv, SUM(cart_users) AS cart_u,
       SUM(pay_new_buyers + pay_old_buyers) AS pay_buyers, SUM(pay_new_buyers) AS new_buyers,
       SUM(refund_amount) AS refund FROM fact_syzt_product, P WHERE stat_date BETWEEN P.s AND P.e)
SELECT '加购率'         AS 指标, ROUND((cart_u     / NULLIF(uv,0)         * 100)::numeric, 2) AS my, 9.19   AS expected FROM agg
UNION ALL SELECT '支付转化率',     ROUND((pay_buyers / NULLIF(uv,0)         * 100)::numeric, 2),               1.79  FROM agg
UNION ALL SELECT '新客成交占比', ROUND((new_buyers / NULLIF(pay_buyers,0) * 100)::numeric, 2),               71.73 FROM agg
UNION ALL SELECT '退款率',       ROUND((refund     / NULLIF(gmv,0)        * 100)::numeric, 2),               31.85 FROM agg
UNION ALL SELECT '客单价',       ROUND((gmv        / NULLIF(pay_buyers,0))::numeric, 2),                     279.23 FROM agg
ORDER BY 1;
```

### 早会上和店长对齐（10 个待定项 #A ~ #J）

把这份「改动记录.md」+「数据口径核对清单.md」转给店长，逐条过一遍。
