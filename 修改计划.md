# HAY Dashboard 全栈修改计划

> 生成日期:2026-04-28
> 范围:server.py / etl/* / schema.sql / bootstrap_recovery.sql / fix_*.{py,sql} / dashboard.html / js/* / js/components/*
> 重点关注:① 用户权限漏洞 ② 环比不稳定 ③ 新增/编辑/导入不稳定 ④ 安全/性能/数据一致性

按 **P0(立即,本周)→ P1(本周内)→ P2(2 周内)→ P3(择期)** 排序。每条带 `文件:行号 / 问题 / 改法`。

---

## ⛔ P0:立即修(高危安全 + 数据彻底失真)

### A. 认证体系全面重做(整个项目最关键)

| # | 位置 | 问题 | 改法 |
|---|---|---|---|
| 1 | `server.py:113-124` | `get_current_user` 仅按 `X-Username` header 字符串查表当登录态。任何人 `curl -H "X-Username: admin"` 即冒充管理员,**所有 RBAC 装饰品**。 | 改用签名 token(JWT 或 server-side session cookie + HttpOnly + Secure + SameSite=Strict)。前端 `api.js:8` 拦截层统一注入 `Authorization: Bearer ...`。 |
| 2 | `server.py:1846 / 1863 / bootstrap_recovery.sql:125-139` | 密码 = `sha256('hay2026')` 无盐无 KDF;8 个账号同口令明文 hash 进 git。 | 改 `bcrypt.hashpw` / `argon2`;首次登录强制改密;清掉 git 历史里的 hash 字符串 + 轮换 Neon 密码。 |
| 3 | `server.py:84-89` | `CORSMiddleware(allow_origins=["*"], allow_methods=["*"])`。 | 生产限定具体域名,移除 `*`。 |
| 4 | `dashboard.html` 整体 | 缺 CSP meta + Vue 从 unpkg 无 SRI。 | 加 `<meta http-equiv="Content-Security-Policy" ...>`;Vue 锁版本 + integrity。 |

### B. 越权 / 提权漏洞(任何人秒变 admin)

| # | 位置 | 问题 | 改法 |
|---|---|---|---|
| 5 | `server.py:1906-1922` `update_user` | **完全无鉴权**,且 body.role 直接落库 → 任何人 PATCH /api/users/{id} 即可把自己升 admin。 | `_user=Depends(require_permission('user.edit'))`;`role` 字段需 `_user.role=='admin'`;不允许给自己改 role。 |
| 6 | `server.py:1845-1858` `register_user` | body.role 默认 viewer 但前端可传 `"admin"` 直通。 | 强制 `role='viewer'`,提权走独立 endpoint。 |
| 7 | `server.py:2863-2944` `/api/refresh-data` | 无鉴权 + 文件名未 `secure_filename` → path traversal 写文件到任意目录;触发 `subprocess.run` 跑 ETL。 | 加 `require_permission('data.import')` + `secure_filename` + 校验 path 不逃出 `dest_dir` + 限速(rate-limit per user)。 |
| 8 | `server.py:1543-1545 + permissions.js:14-25 + AdsPage.js:347` | `isTaskOwner` 用 `display_name in owner` 子串匹配。display_name="李四" 可编辑 owner="李四四" 的任务。 | 前后端统一用 `task.owner_username == user.username` 严格相等;display_name 模糊匹配分支全删。 |
| 9 | `server.py:1443 / 1469 / 1664 / 1789 / 1818 / 1907 / 1955 / 1968 / 2735 / 2838` 等 | 大量写 endpoint 完全无 `require_permission`(`task-periods`, `bulk-instantiate`, `meeting-notes`, `users/preferences`, `plan/category`, `app-state`)。 | 逐个加权限装饰器,本表 P0 阶段先把 admin/manager 区分起来。 |
| 10 | `server.py:2735-2758 + /api/app-state(2828)` | 公开可写,任何人 POST 改全局视图(hiddenPids/customProducts)。 | 加 `require_permission('app_state.write')`;app-state 加 `version` 列做乐观锁。 |
| 11 | `store.js:151 + dashboard.html:151` `_initAuthFromDb` | 失败 fallback 到 `defaultUsers()` 里 `u_admin = ['*']`,网络瞬断即所有访客成 admin。 | 失败显式停留在"未登录"态,**禁止 fallback 到 defaultUsers 里有 `['*']` 的占位账号**;不能识别用户就直接禁用所有写按钮。 |
| 12 | `SettingsPage.js:180-195 + 144-173` | (a) deleteUser 不阻止删最后一个 admin;(b) saveUserModal 允许把自己 admin 降级。 | 加 `if (u.role==='admin' && admins.length<=1) return alert(...)`;`if (u.id===self && u.role==='admin' && newRole!=='admin') return alert('不能把自己降级')`。 |
| 13 | `SettingsPage.js:357-389 + store.js:160-179` `importState` | `Object.assign(APP_STATE, JSON.parse(file))` 无 schema 校验,可注入恶意 users / 占位 currentUserId。 | 白名单字段(actions/customProducts/hiddenPids/imageOverrides/manualDailyData/events 等);**严禁** import users/permissionGroups/currentUserId;校验 stateVersion 走 migrateState;弹 confirm 列出"将覆盖以下 N 项"。 |
| 14 | `AIPage.js:240-244 + SettingsPage.js:528,843` | OpenAI apiKey 明文存 localStorage + UI 直接显示;前端把 HAY 商品名/GMV/ROI 直接 POST 到第三方,无审计。 | AI 调用统一走后端代理 `/api/ai/chat`,key 只在 server;前端绝不持有 key;后端记录 prompt+user_id 审计;脱敏品牌名再发送。 |

### C. 数据彻底失真的 Bug

| # | 位置 | 问题 | 改法 |
|---|---|---|---|
| 15 | `ActionEffectPage.js:44` | `conv_rate = cart/vis*100` —— 转化率公式被复制成了加购率,**全错**。ProductsPage / ComparePage 已修,这里漏掉了。 | `const pb = p.pay_buyers?.[idx] ?? ((p.new_buyers?.[idx]||0)+(p.old_buyers?.[idx]||0)); conv_rate: vis>0 && pb!=null ? pb/vis*100 : null` |
| 16 | `schema.sql:163` | 关键词 UNIQUE 仍是 2 维 `(stat_date, keyword_name)`。新环境部署/`--reset` 后第一次跑会再次漏 24K 数据。 | 改成 `UNIQUE(stat_date, keyword_id, scene_name, keyword_name)`,与 ETL 4 维 key 对齐。 |
| 17 | `schema.sql:319-320` | `idx_kw_date` 被定义两次(fact_wxst_keyword 行 314 + fact_wxst_kw_product 行 320)。Postgres 索引名全库唯一,**第二次 CREATE 直接报错,初始化失败**。 | 行 320 改名 `idx_kw_product_date`。 |
| 18 | `refresh_dashboard.py:354` | `html = html[:start] + new_raw + html[end:]` 中 `end` 未定义。脚本完全跑不通。 | `end = start + len(raw_json_str)`,或直接用 `html[start + len(raw_json_str):]`。 |
| 19 | `etl/etl_load.py:138-150` `--reset` | 会一并 DROP `dim_product`,而 bootstrap_recovery.sql 里手补的 4 个商品 + tasks/users/task_template/task_period/category_audience_plan 不在 schema.sql 里 → 一旦 --reset,业务数据全断。 | `--reset` 改白名单只 TRUNCATE 6 张 fact_*;严禁 DROP dim_product / users / tasks 等;在脚本顶部明确列出 RESETTABLE 集合并 `assert table_name in RESETTABLE`。 |
| 20 | `etl/etl_load.py:336-348 / 433 / 505 / 588 / 662 / 720` ON CONFLICT DO NOTHING | 同一 (date, pid) 第二次跑应该用新值覆盖(平台经常补昨天数据),但 DO NOTHING 让旧值长期固化 → 数据一直对不上后台。 | 改 `DO UPDATE SET col=EXCLUDED.col, ..., source_file=EXCLUDED.source_file` |
| 21 | `bootstrap_recovery.sql:136-139` | users 表 ON CONFLICT DO UPDATE 反复重置 role + permissions。运营在 GUI 里改的权限,店长再跑一次 SQL 就回滚。 | 改 `DO NOTHING`;或 `DO UPDATE SET display_name = COALESCE(NULLIF(users.display_name,''), EXCLUDED.display_name)` 之类的非破坏合并。 |
| 22 | `bootstrap_recovery.sql:215-227` | `is_current` 唯一性靠 UPDATE WHERE label!=...,无数据库约束,周期切换时极易出现多个 `is_current=TRUE`。 | 加部分唯一索引 `CREATE UNIQUE INDEX ON task_period (is_current) WHERE is_current = TRUE`。 |

---

## 🔥 P1:本周内修(环比不稳定 / CRUD 不稳定 / 重要功能 bug)

### D. 环比口径统一(用户反馈"环比不稳定"的根因)

| # | 位置 | 问题 | 改法 |
|---|---|---|---|
| 23 | `server.py:2439-2445 _prev_range` vs `server.py:1169 tasks/with-metrics` | 两套环比口径并存:ranking/kpi 走"等长前移",任务卡走"上一个 task_period"。前端两处看到的环比百分比不一致。 | 抽公共函数 `_prev_range(s, e, mode='aligned'\|'period')`,在 API 入参加 `compare_mode` 让前端切换;**默认值在 config.js 统一**。 |
| 24 | `server.py:2439, 2462` | `e = date.today()`,当周未结束(e=今天)时等长前移会拿到不对齐的"半周",百分比夸张。 | SQL 端按比例归一:`(cur_value/days_cur) vs (prev_value/days_prev)`,或 API 显式返回 `days_cur, days_prev` 让前端归一展示。 |
| 25 | `compute.js:150,229,234 / server.py:2630-2633,2544-2547,1293-1296` | `pct(a,b)` 当 prev=0 / null 一律返回 null,前端无法区分"没数据"和"上期 0 当期>0"。 | 拆三态:`null`(任一缺数 → "—"),`Infinity`(prev=0 且 cur>0 → "↑NEW"),数字。前端 chgTxt 据此渲染。 |
| 26 | `OverviewPage.js:244 + 450 / 多处 KPI 卡 / Compare / Products` | -100% 隐藏只在 `AdsPage.js:1124` 落实,其他位点都漏(违反改动记录第 15 条)。 | 抽公共 helper `shouldShowDiff(cur, diff_pct)`(cur ≠ null && cur ≠ 0 && Math.abs(diff)<999),**所有展示环比的位点统一调用**。 |
| 27 | `compute.js:131-137 / compute.js:71` | 推广 CTR 用算术平均(ctr_sum/n),与 AdsPage:773 的加权 adsCtr 不一致;数据口径核对清单 §1 已点名禁用算术平均。 | 改为 `SUM(clicks)/SUM(imps)*100`;ETL 侧补 `clicks/imps` 列(server.py:6 已加,核对前端是否消费到)。 |
| 28 | `OverviewPage.js:97-141 loadRank` | 三层兜底无 AbortController + 无超时,快速切换分类发多次重叠请求。 | 加 `AbortController`,fallback 超时 2s;切分类时 abort 上一次。 |
| 29 | `etl/refresh_dashboard.py:138-169` | 关键词去重仍是 `(d, name)` 2 维,与 etl_load.py 的 4 维不一致。 | 同步加 scene 维度 `key=(d, name, scene_name)`。 |

### E. CRUD / 新增编辑导入稳定性

| # | 位置 | 问题 | 改法 |
|---|---|---|---|
| 30 | `server.py:db()(行 92)` 和所有写接口 | 无 try/except + rollback。INSERT 中途异常时事务残留 idle-in-transaction → 后续请求 hang。 | `db()` 改 `try: yield conn; except: conn.rollback(); raise; finally: conn.close()`。 |
| 31 | `server.py:1505-1516, 1642-1653, 2735-2758` create/update 路由 | 直接 `cur.execute + conn.commit()`,无业务校验。 | 套 try/except,失败 rollback + raise HTTPException。 |
| 32 | `server.py:1469-1502 bulk_instantiate_tasks` | 不 JOIN dim_product,可灌孤儿任务(product_id 不存在)。 | CTE 加 `JOIN dim_product USING(product_id)`;response 返回 `skipped:[...]`。 |
| 33 | `server.py:1115-1126, 1546-1554, 1666-1677, 1907-1922` | 动态拼 `set_clause` 字段名,虽然现在 Pydantic 限了,但无白名单一旦扩展即 SQL 注入。 | 加 `ALLOWED_COLUMNS` 白名单 `assert k in ALLOWED`。 |
| 34 | `server.py:2864-2890 上传分流` | 用中文字节匹配 `b"\xe8\x8a\xb1\xe8\xb4\xb9"` 判断"花费",不严谨,文件名带不带"推广"决定路径。 | 前端按 endpoint 显式传 `?kind=ads\|syzt\|content`,后端只校验。 |
| 35 | `AdsPage.js:381-411 saveCellEdit` | 失败仅 alert,`editingCell` 不重置,UI 卡死;并发编辑 race。 | 失败也 `editingCell.value=null;cellDraft 重置`;乐观更新 + 失败回滚原值;给每个 task.id 维护 `_pending Set`,reload 时跳过 pending。 |
| 36 | `AdsPage.js:415-431 deleteTaskRow` | 无 inflight 锁,双击连发两次 DELETE。 | `deletingIds Set` 防双击;按钮 `:disabled`。 |
| 37 | `ProductsPage.js:229-317 saveTask/saveXhs/saveEvent` | 无 saving 绑定到 button `:disabled`,慢网下重复提交。 | 所有 save modal 按钮统一 `:disabled="modal.saving"`;saveEvent 改异步并加 saving。 |
| 38 | `ProductsPage.js:300-311 saveXhs` | 直接修改 RAW.xhs_notes 且 `agg.inter += newNote.inter`(undefined),NaN 污染聚合。 | `(newNote.likes\|\|0)` 防御;inter 在前端用 `likes+collect+comments` 重算;saveXhs 后 `_loadRawFromAPI()` 全量 reload。 |
| 39 | `AdsPage.js:495-520 doBulkAssign` | 部分成功不可见,失败时 selectedTaskIds 不清。 | 后端返回 `succeeded_ids[]`,前端按这个剔除;失败 toast 显示哪些没成功。 |
| 40 | `AdsPage.js:606-614 openBulkCreate` | mode='all'+'custom' 下默认全选模板,容易意外建几十条任务。 | 切 mode 时清空 `selectedTplIds`;custom 模式强制至少选 1 个。 |
| 41 | `AdsPage.js:678-687 onCommentImagePick` | 1MB 限制 + 不校验 MIME 类型。 | `if (!file.type.startsWith('image/'))` 拒绝;限 5MB + canvas resize。 |
| 42 | `SettingsPage.js:316-340 saveAudiencePlan` | 不校验 4 项总和=100。 | `audiencePlanTotal === '100.0'` 才允许提交。 |
| 43 | `SettingsPage.js:390-424 saveProductModal` | 改 pid 时不迁移 events / xhs / actions / tasksByPid,历史记录全丢。 | 改 pid 时遍历相关 state 替换 pid;最好做后端 `/api/products/{old_pid}/migrate-to/{new_pid}`。 |
| 44 | `SettingsPage.js:588-604 / store.js:226 runImport` | 无进度、无超时、无 abort、断网无提示;关闭 tab 时 400ms debounce 内的 push 丢失。 | XHR/fetch 进度;`AbortController` + 60s 超时;`window.addEventListener('beforeunload', ()=>navigator.sendBeacon(...))`。 |
| 45 | `etl/etl_load.py:286-294 load_dim_product` ON CONFLICT | UPDATE 时把 inventory 一律刷为 0,title 也被 xlsx 短名覆盖,bootstrap_recovery.sql 手补的 4 个商品被冲。 | inventory 改 `COALESCE(EXCLUDED.inventory, dim_product.inventory)`;title/category 只在 INSERT 写;UPDATE 集仅含 spu_id。 |
| 46 | `etl/etl_load.py:415-476 load_wxst_product` | 副 SKU 没合并到主 SKU,前端按 product_id 查会漏副 SKU 花费。 | 二选一:① ETL 入库前 spu_id() 映射 product_id(保留 raw_product_id 列);② server.py 所有视图强制 `JOIN dim_product GROUP BY spu_id`。**必须两边对齐选一边**。 |
| 47 | `etl/etl_load.py:644-701 + schema.sql` | `fact_wxst_content` 表只在 bootstrap_recovery.sql 建,schema.sql 没有 → 全新环境跑 ETL 直接报 relation does not exist。 | 把 CREATE TABLE 复制到 schema.sql。 |
| 48 | `schema.sql:232-240, 255-267` | users.preferences 列、tasks 业务唯一键不在 schema.sql,只靠 fix_*.sql 临时打补丁,不幂等。 | 直接合并到 schema.sql:`users` 加 `preferences JSONB DEFAULT '{}'`;`tasks` 加 `UNIQUE(product_id, time_range_label, detail, owner)`。 |

---

## 🟡 P2:2 周内(健壮性 + 边界 case)

### F. 数据/口径

| # | 位置 | 问题 | 改法 |
|---|---|---|---|
| 49 | `server.py:2027-2028` | `MAX(s.year_cum_pay)` 多 SKU 合并 SPU 时 GMV 偏低。 | 子查询 per-product `DISTINCT ON (product_id) ... ORDER BY product_id, stat_date DESC` 再 SUM。 |
| 50 | `server.py:1241-1253` | content_visits 把全店一个值塞每个商品。 | 等 `fact_wxst_content` 加 product_id 维表;短期前端 hide 此列或加角标。 |
| 51 | `server.py:1262, 2349` | 日期 BETWEEN 字符串/timestamp 混用,可能漏当天数据。 | `publish_time >= %s::date AND publish_time < (%s::date + 1)`。 |
| 52 | `server.py:501 + 2776` | `datetime.now()` 无时区 vs `AT TIME ZONE 'Asia/Shanghai'` 混用。 | 统一 `datetime.now(ZoneInfo('Asia/Shanghai'))`。 |
| 53 | `etl/etl_load.py:113-127 safe_pct` | 阈值 1.5 启发式判 % 单位,不可靠(1.2% 会被误判)。 | 看原始字符串末尾是否有 `%`;丢启发式。 |
| 54 | `指标口径整理.py:174` | 转化率口径写成"加购人数 / 访客数"(和加购率一模一样)。 | 改成"支付买家 / 访客数";长期把 server.py 的实际 SQL 通过 `/api/metric-defs` 暴露,xlsx 从 API 生成,避免漂移。 |
| 55 | `compute.js:212` | 浮点 toFixed 字符串往返,边界 4.95/9.95 摇摆。 | `Math.round((ap-pp)*10)/10` 一致化。 |
| 56 | `utils.js:28-35` | `prevRange` 用 toISOString,跨时区/DST 偏 1 天隐患。 | 纯字符串解析 `'YYYY-MM-DD'`。 |

### G. 错误处理 / 性能

| # | 位置 | 问题 | 改法 |
|---|---|---|---|
| 57 | `server.py:113-124, 504-513` | 全局 try/except 吞异常返 None / 空对象,DB 闪断时所有请求"匿名 viewer"或显示 0 销售。 | 区分 503/500/部分失败;打日志含 traceback;前端 api.js 区分 401/403/5xx。 |
| 58 | `api.js:23-50` | 401/403/500 全部走 mock fallback,用户看不到错误。 | 401 跳登录;403 toast "无权限";5xx 提示"服务异常"+重试;只有真正 network error 才 fallback。 |
| 59 | `api.js:23-25` | `start='2026-04-15'` 写死。 | `subDays(RAW.data_end, 6)` 之类,用 RAW.data_end 兜底。 |
| 60 | `server.py:1149-1324 get_tasks_with_metrics` | 循环内多次查 DB,N+1 问题(25 SPU 时 5+ 秒)。 | 单 query 用 `CASE WHEN stat_date BETWEEN ...` 一次出当期+上期。 |
| 61 | `server.py:46-78 create_tables` | DB 不通时 warning 跳过,服务"启动成功"但所有请求 500。 | RuntimeError fail-fast,配 supervisor 重启。 |
| 62 | `server.py:2864 subprocess.run timeout=300` | FastAPI 同步 endpoint 占 worker 5 分钟。 | 改 BackgroundTasks 或 Celery,前端轮询状态。 |
| 63 | `server.py:1924-1938 delete_user` | 不清外键,task_comment 留 `author_username='alice'` 但 users 没 alice,前端反查头像/display 失败。 | 删用户前 `UPDATE task_comment SET author_username='[deleted]'`;或 users 软删。 |
| 64 | `server.py:1374-1403 update_task_template propagate` | 一次性把所有未来任务 owner 强行改写,无 audit、不可回滚。 | 写 `audit_log`;UI 弹确认框列出"将影响 N 个任务"。 |
| 65 | `server.py:1944` `record_page_view` | 不验 username 真实存在,可灌脏数据。 | 校验 user 存在再写。 |
| 66 | `AIPage.js:67-69` | storage 监听器未在 onUnmounted 移除,内存泄漏 + 重复执行 loadConfig。 | `const handler=...; onMounted(add); onUnmounted(remove)`。 |
| 67 | `AIPage.js:225-272 / 282-285` | 无频率限制,连点 5 个一键分析按钮 → 5 倍 token 消耗。 | runSSE 入口检查 chatLoading;按用户加节流(每 30s N 次)。 |
| 68 | `OverviewPage.js:182-185 getWeekKey` | 跨年第 53 周边缘 case 错。 | 用 ISO-8601 周标准。 |
| 69 | `InteractiveTrendChart.js:55,118` | `xOf(-1)/N` 产生 NaN 画图外;tooltip 不防左下越界 + portal。 | 边界处理同 eventLayout;tooltip teleport 到 body。 |
| 70 | `InteractiveTrendChart.js:104` `gapBridgeD = _ => []` | 死代码。 | 删掉 + 模板里的 v-for bridge。 |
| 71 | `App.js:37, 47-51` | 登录态 sessionStorage 无 token;logout 不清 APP_STATE.currentUserId / hay_current_user。 | 与 P0#1 一并改 token;logout 全清。 |
| 72 | `SettingsPage.js:201-202 vs App.js:37` | togglePerm 取 `localStorage('hay_current_user')`,App 写 `sessionStorage('hay_user')`,**两套来源不一致**。 | 统一一处(建议 sessionStorage)。 |
| 73 | `permissions.js:7 + server.py:142 require_permission` | role 大小写比较不一致,'Admin' 前后端判定不同。 | 统一 `role.toLowerCase()==='admin'`。 |

---

## 🟢 P3:择期(代码味道 / 可优化)

| # | 位置 | 改法 |
|---|---|---|
| 74 | `server.py:30, 2949` 静态文件无 Cache-Control | 加 `headers={"Cache-Control":"no-cache"}` |
| 75 | `server.py:163` `CAMPAIGN_START='2026-04-08'` 硬编码 | 移到 env / settings 表 |
| 76 | `server.py:1032-1053, 2664-2692, 2801-2817` `_ensure_*_table()` 散落 | 集中到 startup,加迁移版本号(Alembic) |
| 77 | `server.py:179-513 get_raw_data` 一坨 500 行 | 拆函数 + 加缓存 |
| 78 | `server.py:1794, 1821, 1889, 2738, 2822, 2840` 函数内重复 `import json` | 提到顶部 |
| 79 | `etl/etl_load.py:321-326` monkey-patch psycopg2 全局副作用 | 显式应用 _clean_row,不改全局 |
| 80 | `etl/etl_load.py:85, 190` `zipfile.ZipFile` 未 with | `with zipfile.ZipFile(path) as zf:` |
| 81 | `fix_users_cleanup.sql:29-34` 按硬编码 id DELETE | 改按 username |
| 82 | `fix_names.py / fix_products.py` 直接字符串替换 dashboard.html | 写 .tmp 再 rename;失败回滚 |
| 83 | `seed_legacy_local.py:14-16` Neon DSN 明文进 git | 强制 env var,清 git 历史 + 轮换密码 |
| 84 | `bootstrap_recovery.sql:241-272` 多个 INSERT 不在事务内 | 包 BEGIN/COMMIT |
| 85 | `bootstrap_recovery.sql:94` `template_id REFERENCES task_template` 无 ON DELETE | `ON DELETE SET NULL` |
| 86 | `schema.sql:209-221 fact_xhs_note` `note_url` UNIQUE 但 nullable | NOT NULL 或 `UNIQUE NULLS NOT DISTINCT`(PG15+) |
| 87 | `schema.sql:167-188 fact_traffic` 列与 ETL insert 列不匹配 | 删未用列或 ETL 补字段 |
| 88 | `schema.sql:308-318` 缺 `fact_paid_promo.product_id`、`fact_traffic.source_l1` 索引 | 加索引 |
| 89 | `compute.js / config.js` 权限码列表与 server.py 不一致 | 标记或删除前端有定义、后端没接的权限 |
| 90 | `config.js:62-66 metricTip` 死函数 | 删 |
| 91 | `utils.js:5 deepClone = JSON.parse(JSON.stringify)` | `structuredClone(v)` |
| 92 | `utils.js:6-9 writeStore` 无 try/catch | 加 quota 异常捕获 |
| 93 | `utils.js:38-40 fPct/chgTxt` `Number('abc')=NaN → 'NaN%'` | `Number.isFinite(x) ? ... : '—'` |
| 94 | `ProductsPage.js:65-67 fmt(0,'pct')='—'` | 改 v==null 才 '—',0 显示 0.00% |
| 95 | `ComparePage.js:84 fallback launchRange` | UI 标签提示"(全期 fallback)" |
| 96 | `SettingsPage.js:443 vs ActionEffectPage.js:133` ACTION_TYPES 不一致 | 抽到 utils.js 共享常量 |
| 97 | `App.js:105-122 setPreset` 用 `new Date()` 跨日错位 | 统一用 RAW.data_end |
| 98 | `App.js:260-265` 路由切换组件 modal 状态不还原 | modal 一律 ref 局部 |
| 99 | `OverviewPage.js:90-95 localRank 上期窗口` 未处理 launch_date 之前 | < launch_date 时直接 prev_top=[] |
| 100 | `AIPage.js:115-117 split(' ')` 全角空格 | `split(/[\s　]/)` |
| 101 | `compute.js:226-229 computeAll` 未 `await fetchChannelRatios` | api.js 在 catch 后先 await fetchChannelRatios 再 computeAll |
| 102 | `config.js:19-20 AUDIENCE_RATIO` 硬编码 | 标注 snapshot 日期 + 月度校准;fallback 时 throw |
| 103 | `etl_load.py:160-184 populate_dim_date` 早退条件 | 加 max(date) 起始,避免每年重灌全年 |
| 104 | `fix_users_cleanup_round2.sql / fix_dedup_tasks.sql:28` ORDER BY 优先级 | 主排序键改 `updated_at DESC` |
| 105 | `dashboard.html` 全是 inline style/script | 逐步迁到 class,最终去 'unsafe-inline' |

---

## 📋 执行建议

### 第 1 周(P0,本周)
- 第 1-2 天:**认证体系** —— JWT/session、密码 hash 重做、CORS。这块改完很多其他 P0 自动收敛。
- 第 3 天:**越权修复** —— update_user / register_user / refresh-data / isTaskOwner / app-state(#5-#10、#12)。
- 第 4 天:**默认 admin fallback + importState 校验 + AI 代理**(#11、#13、#14)。
- 第 5 天:**数据失真硬 bug** —— ActionEffectPage conv_rate / schema 关键词 UNIQUE / 索引名 / refresh_dashboard.py NameError / etl --reset 白名单 / ON CONFLICT DO UPDATE / users ON CONFLICT DO NOTHING / task_period.is_current 索引(#15-#22)。

### 第 2 周(P1)
- 周一:统一环比口径(#23-#28)+ refresh_dashboard 4 维 key(#29)。
- 周二-周三:server.py 写接口事务化、白名单、bulk_instantiate join、上传分流(#30-#34)。
- 周四-周五:前端 saveCellEdit / deleteTaskRow / save modals 防双击 + 错误回滚 + 进度 + abort(#35-#44)。
- 任意时间:dim_product / wxst_product / fact_wxst_content / schema 完善(#45-#48)。

### 第 3 周(P2)
- 数据口径(#49-#56)
- 错误处理 + 性能(#57-#73)

### 之后(P3)
- 105 条代码味道/优化,按 sprint 分批做。

---

## 🚨 一定要先达成的"共识"(给店长 / 团队 P0 拍板)

修代码之前请先和店长确认 4 件事(改动记录里的待定项):

1. **环比口径**:总览 / 投放 / 任务三处的"上期"定义统一吗?选「等长前移」「上一完整周」「让用户切」?(数据口径核对清单 §8 / 改动记录 #D)
2. **GMV 用支付金额还是下单金额?**(口径核对清单 §3 / 改动记录 #A)
3. **退款 / 净 GMV / 新客占比 KPI 要不要加?**(口径核对清单 §14 / 改动记录 #B)
4. **副 SKU 合并 13 对中,Slit/Tray Table、字母 Logo Cotton Bag 要不要合 SPU?**(口径核对清单 §10 / 改动记录 #F、#G)

这 4 项不定 → 数据口径修了也没意义。

---

## 风险提示

- 改完认证(P0#1)后**所有现有 sessionStorage 登录态会失效**,需要全员重新登录,提前发通知。
- `etl/etl_load.py --reset` 白名单(P0#19)落地前,**绝不要再跑 `--reset`**(会清掉 dim_product 手补的 4 个商品 + 所有 tasks/users)。
- `bootstrap_recovery.sql` users 块改 DO NOTHING(P0#21)前,**任何人在 GUI 改的权限只要店长再跑一次 SQL 就被回滚**——已经发生过类似事故(改动记录 #30)。
- AI apiKey 改后端代理(P0#14)期间,前端 AIPage 会失效几分钟,部署前先把后端接口部署上去再切前端。
